import React from "react";
import { NumberInputStepper as NBNumberInputStepper } from "native-base";
export const NumberInputStepper = ({ ...props }: any) => {
  return <NBNumberInputStepper {...props}></NBNumberInputStepper>;
};

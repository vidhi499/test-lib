export { AppProvider } from './AppProvider';
export { extendTheme } from 'native-base';
export {
  useColorMode,
  useColorModeValue,
  useAccessibleColors,
} from 'native-base';

export type { INativebaseConfig } from 'native-base';

export type {
  StorageManager,
  ColorMode,
  ColorModeOptions,
  IColorModeProviderProps,
  IColorModeContextProps,
} from 'native-base';
export { ToastProvider } from 'native-base';
